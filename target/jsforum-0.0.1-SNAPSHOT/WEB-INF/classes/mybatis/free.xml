<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="free">
	<select id="list" resultType="Free">
		SELECT * FROM free
	</select>
	<delete id="delete">
		DELETE FROM free WHERE num=#{num}
	</delete>
	<insert id="add">
		INSERT INTO free (name, pub_date, id, contents)
		VALUES (#{name}, now(),#{userId},#{contents})
	</insert>
	<update id="update">
		UPDATE free SET name=#{name}, contents=#{contents}
		WHERE num=#{num}
	</update>
	<select id="item" resultType="Free">
		SELECT * FROM free left join  user on free.id = user.id WHERE num=#{num}

	</select>
</mapper>